import { ParseRelationPipe } from './parse-relation.pipe';

describe('ParseRelationPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseRelationPipe();
    expect(pipe).toBeTruthy();
  });
});
