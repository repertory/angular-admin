import { ParsePointerPipe } from './parse-pointer.pipe';

describe('ParsePointerPipe', () => {
  it('create an instance', () => {
    const pipe = new ParsePointerPipe();
    expect(pipe).toBeTruthy();
  });
});
