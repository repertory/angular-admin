import {ParseObjectPipe} from './parse-object.pipe';

describe('ParseObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
